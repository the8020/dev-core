version = 1
command = "dev-core.sandbox.shell"
program = "sandbox-shell"
summary = "Execute inside a development sandbox"
usage = "<user-id> [--command <command>]"
mutates_state = true
restart_behavior = "none"
[[examples]]
command = "dev-core.sandbox.shell alice --command pwd"
